// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System.Collections.Generic;

namespace Microsoft.PowerBI.Api.Models
{
    /// <summary> Defines the user identity and roles. For more information, see [Row-level security with Power BI Embedded](/power-bi/developer/embedded/embedded-row-level-security). </summary>
    public partial class EffectiveIdentity
    {
        /// <summary> Initializes a new instance of <see cref="EffectiveIdentity"/>. </summary>
        public EffectiveIdentity()
        {
            Datasets = new ChangeTrackingList<string>();
            Roles = new ChangeTrackingList<string>();
            Reports = new ChangeTrackingList<string>();
        }

        /// <summary> Initializes a new instance of <see cref="EffectiveIdentity"/>. </summary>
        /// <param name="username"> The effective username within a token that applies row-level security rules. For an on-premises model, the username can contain alphanumeric or any of the following characters `.`, `-`, `_`, `!`, `#`, `^`, `~`, `\\`, `@`. For cloud models, the username can contain any ASCII character. For either model, the username length must not exceed 256 characters, and the username shouldn't contain spaces. </param>
        /// <param name="auditableContext"> The EffectiveIdentity auditable context. If this parameter is provided and isn't empty, it will enable auditing of the EffectiveIdentity and its value will be set to the username in the audit record. Otherwise, the EffectiveIdentity context will be omitted from the GenerateToken audit record. </param>
        /// <param name="datasets"> An array of datasets for which this identity applies. </param>
        /// <param name="roles"> An array of row-level security (RLS) roles within a token that applies RLS rules. An identity can contain up to 50 roles. A role can contain any character except `,`, and its length must not exceed 50 characters. </param>
        /// <param name="customData"> [Custom data](/power-bi/developer/embedded/embedded-row-level-security#using-the-customdata-feature) that's used to apply row-level security rules. Supported for live connection to Azure Analysis Services models and cloud models only. </param>
        /// <param name="identityBlob"> A blob that specifies an [identity](/power-bi/developer/embedded/embedded-row-level-security#token-based-identity-sdk-additions). Only supported for datasets with a DirectQuery connection to Azure SQL. </param>
        /// <param name="reports"> An array of reports for which this identity applies. Only supported for paginated reports. </param>
        internal EffectiveIdentity(string username, string auditableContext, IList<string> datasets, IList<string> roles, string customData, IdentityBlob identityBlob, IList<string> reports)
        {
            Username = username;
            AuditableContext = auditableContext;
            Datasets = datasets;
            Roles = roles;
            CustomData = customData;
            IdentityBlob = identityBlob;
            Reports = reports;
        }

        /// <summary> The effective username within a token that applies row-level security rules. For an on-premises model, the username can contain alphanumeric or any of the following characters `.`, `-`, `_`, `!`, `#`, `^`, `~`, `\\`, `@`. For cloud models, the username can contain any ASCII character. For either model, the username length must not exceed 256 characters, and the username shouldn't contain spaces. </summary>
        public string Username { get; set; }
        /// <summary> The EffectiveIdentity auditable context. If this parameter is provided and isn't empty, it will enable auditing of the EffectiveIdentity and its value will be set to the username in the audit record. Otherwise, the EffectiveIdentity context will be omitted from the GenerateToken audit record. </summary>
        public string AuditableContext { get; set; }
        /// <summary> An array of datasets for which this identity applies. </summary>
        public IList<string> Datasets { get; }
        /// <summary> An array of row-level security (RLS) roles within a token that applies RLS rules. An identity can contain up to 50 roles. A role can contain any character except `,`, and its length must not exceed 50 characters. </summary>
        public IList<string> Roles { get; }
        /// <summary> [Custom data](/power-bi/developer/embedded/embedded-row-level-security#using-the-customdata-feature) that's used to apply row-level security rules. Supported for live connection to Azure Analysis Services models and cloud models only. </summary>
        public string CustomData { get; set; }
        /// <summary> A blob that specifies an [identity](/power-bi/developer/embedded/embedded-row-level-security#token-based-identity-sdk-additions). Only supported for datasets with a DirectQuery connection to Azure SQL. </summary>
        public IdentityBlob IdentityBlob { get; set; }
        /// <summary> An array of reports for which this identity applies. Only supported for paginated reports. </summary>
        public IList<string> Reports { get; }
    }
}
