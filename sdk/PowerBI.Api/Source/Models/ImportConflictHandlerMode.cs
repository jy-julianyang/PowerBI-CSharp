// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

namespace Microsoft.PowerBI.Api.Models
{
    /// <summary> The ImportConflictHandlerMode. </summary>
    public enum ImportConflictHandlerMode
    {
        /// <summary> If a dataset with the same name already exists, the import operation will create a new dataset with the same name. </summary>
        Ignore,
        /// <summary> If a dataset with the same name already exists, the import operation will be cancelled. </summary>
        Abort,
        /// <summary> If a dataset with the same name already exists, the import operation will replace the existing dataset with the new one. The import operation will fail if there's no conflict or if there's more than one existing dataset with the same name. </summary>
        Overwrite,
        /// <summary> If a dataset with the same name already exists, the import operation will replace the existing dataset with the new one. The import operation will fail if there's more than one existing dataset with the same name. </summary>
        CreateOrOverwrite,
        /// <summary> If a dataflow with the same name already exists, the import operation will generate a new unique name for the new dataflow. </summary>
        GenerateUniqueName
    }
}
