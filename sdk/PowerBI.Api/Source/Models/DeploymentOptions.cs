// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

namespace Microsoft.PowerBI.Api.Models
{
    /// <summary> Deployment configuration options. Can be specified either for the entire deployment or for a specific Power BI item (such as a report or dashboard). If both are specified, only the deployment options for the Power BI item are used. </summary>
    public partial class DeploymentOptions
    {
        /// <summary> Initializes a new instance of <see cref="DeploymentOptions"/>. </summary>
        public DeploymentOptions()
        {
        }

        /// <summary> Initializes a new instance of <see cref="DeploymentOptions"/>. </summary>
        /// <param name="allowCreateArtifact"> Whether creating a new Power BI item (such as a report or a dashboard) in the target stage workspace is allowed. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        /// <param name="allowOverwriteArtifact"> Whether overwriting a Power BI item (such as a report or a dashboard) in the target stage workspace is allowed. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        /// <param name="allowSkipTilesWithMissingPrerequisites"> Whether to skip tiles that don't have a model or a report in the target stage workspace. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        /// <param name="allowPurgeData"> Whether to delete all data from the target Power BI item (such as a report or a dashboard) when there's a schema mismatch. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        /// <param name="allowTakeOver"> Whether to allow overriding the previous paginated report owner and becoming the owner of the paginated report. Applicable when deploying a paginated report to a stage that already contains a copy of the paginated report that isn't owned by you. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        /// <param name="allowOverwriteTargetArtifactLabel"> Whether the label of a target Power BI item (such as a report or a dashboard) can be changed. The label gets changed when the source is protected but the target isn't. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </param>
        internal DeploymentOptions(bool? allowCreateArtifact, bool? allowOverwriteArtifact, bool? allowSkipTilesWithMissingPrerequisites, bool? allowPurgeData, bool? allowTakeOver, bool? allowOverwriteTargetArtifactLabel)
        {
            AllowCreateArtifact = allowCreateArtifact;
            AllowOverwriteArtifact = allowOverwriteArtifact;
            AllowSkipTilesWithMissingPrerequisites = allowSkipTilesWithMissingPrerequisites;
            AllowPurgeData = allowPurgeData;
            AllowTakeOver = allowTakeOver;
            AllowOverwriteTargetArtifactLabel = allowOverwriteTargetArtifactLabel;
        }

        /// <summary> Whether creating a new Power BI item (such as a report or a dashboard) in the target stage workspace is allowed. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowCreateArtifact { get; set; }
        /// <summary> Whether overwriting a Power BI item (such as a report or a dashboard) in the target stage workspace is allowed. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowOverwriteArtifact { get; set; }
        /// <summary> Whether to skip tiles that don't have a model or a report in the target stage workspace. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowSkipTilesWithMissingPrerequisites { get; set; }
        /// <summary> Whether to delete all data from the target Power BI item (such as a report or a dashboard) when there's a schema mismatch. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowPurgeData { get; set; }
        /// <summary> Whether to allow overriding the previous paginated report owner and becoming the owner of the paginated report. Applicable when deploying a paginated report to a stage that already contains a copy of the paginated report that isn't owned by you. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowTakeOver { get; set; }
        /// <summary> Whether the label of a target Power BI item (such as a report or a dashboard) can be changed. The label gets changed when the source is protected but the target isn't. If this option isn't set to `true` when it's required for deployment, the deployment will fail. </summary>
        public bool? AllowOverwriteTargetArtifactLabel { get; set; }
    }
}
